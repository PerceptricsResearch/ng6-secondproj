import { WlbdirectiveDirective } from './wlbdirective.directive';

describe('WlbdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new WlbdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
